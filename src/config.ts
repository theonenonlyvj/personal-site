// Contact form endpoint — a Google Apps Script Web App that appends each
// submission as a row in a Google Sheet you own (see apps-script/README.md).
// Paste the deployed script's /exec URL here. Empty string = form disabled.
export const CONTACT_ENDPOINT = ''
